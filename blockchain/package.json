{
  "name": "pharmascan-blockchain",
  "version": "1.0.0",
  "description": "Smart contracts for PharmaScan",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.4.0",
    "ts-node": "^10.9.2",
    "@types/node": "^20.11.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
